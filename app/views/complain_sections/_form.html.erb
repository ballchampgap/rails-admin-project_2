<%= form_with(model: complain_section) do |form| %>
  <% if complain_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(complain_section.errors.count, "error") %> prohibited this complain_section from being saved:</h2>

      <ul>
        <% complain_section.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :section %>
    <%= form.text_field :section %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
